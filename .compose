#!/bin/bash
# Auto-detect docker compose command (v2 plugin or v1 binary).
if docker compose version &> /dev/null 2>&1; then
    echo "docker compose"
elif command -v docker-compose &> /dev/null 2>&1; then
    echo "docker-compose"
else
    echo "❌ Docker Compose not found!" >&2
    exit 1
fi
