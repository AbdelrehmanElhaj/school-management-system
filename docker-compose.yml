services:
  db:
    image: postgres:15
    container_name: school-db
    environment:
      - POSTGRES_USER=odoo17
      - POSTGRES_PASSWORD=odoo17
      - POSTGRES_DB=postgres
      - PGDATA=/var/lib/postgresql/data/pgdata
    volumes:
      - school-db-data:/var/lib/postgresql/data/pgdata
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U odoo17"]
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - internal

  web:
    image: odoo:17.0
    container_name: school-odoo
    depends_on:
      db:
        condition: service_healthy
    expose:
      - "8069"
    environment:
      - HOST=db
      - USER=odoo17
      - PASSWORD=odoo17
      # nginx-proxy routing
      - VIRTUAL_HOST=school.hdrelhaj.com
      - VIRTUAL_PORT=8069
      # Let's Encrypt
      - LETSENCRYPT_HOST=school.hdrelhaj.com
      - LETSENCRYPT_EMAIL=a.elhaj@proptech.sa
    volumes:
      - school-web-data:/var/lib/odoo
      - ./config:/etc/odoo
      - ./addons:/mnt/extra-addons
      - ./logs:/var/log/odoo
    restart: unless-stopped
    networks:
      - internal
      - web

volumes:
  school-web-data:
    driver: local
  school-db-data:
    driver: local

networks:
  internal:
    driver: bridge
  web:
    external: true
