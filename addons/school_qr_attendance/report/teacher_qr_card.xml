<?xml version="1.0" encoding="utf-8"?>
<odoo>

  <record id="action_report_teacher_qr_card" model="ir.actions.report">
    <field name="name">بطاقة QR للمعلم</field>
    <field name="model">school.teacher</field>
    <field name="report_type">qweb-pdf</field>
    <field name="report_name">school_qr_attendance.report_teacher_qr_card</field>
    <field name="report_file">school_qr_attendance.report_teacher_qr_card</field>
    <field name="binding_model_id" ref="school_management.model_school_teacher"/>
    <field name="binding_type">report</field>
    <field name="paperformat_id" ref="base.paperformat_euro"/>
  </record>

  <template id="report_teacher_qr_card">
    <t t-call="web.html_container">
      <t t-foreach="docs" t-as="teacher">
        <t t-call="web.basic_layout">
          <t t-set="o" t-value="teacher"/>
          <div style="
            width: 148mm; height: 105mm;
            border: 2px solid #0f3460;
            border-radius: 8px;
            display: flex;
            flex-direction: column;
            font-family: 'Segoe UI', Tahoma, sans-serif;
            overflow: hidden;
            margin: auto;
            page-break-after: always;
            direction: rtl;
          ">
            <!-- Header bar -->
            <div style="background:#0f3460; color:white; padding:6px 12px; display:flex; align-items:center; gap:8px;">
              <span style="font-size:1.2rem; font-weight:bold;">👨‍🏫 بطاقة هوية معلم</span>
              <span style="margin-right:auto; font-size:0.75rem; opacity:0.7;">
                <t t-esc="teacher.school_id.name or 'المدرسة'"/>
              </span>
            </div>
            <!-- Body -->
            <div style="display:flex; flex:1; padding:10px; gap:10px; align-items:center;">
              <!-- Photo -->
              <div style="flex-shrink:0;">
                <t t-if="teacher.photo">
                  <img t-att-src="'data:image/png;base64,' + (teacher.photo.decode('utf-8') if isinstance(teacher.photo, bytes) else teacher.photo)"
                       style="width:60px; height:60px; object-fit:cover; border-radius:50%; border:2px solid #e94560;"/>
                </t>
                <t t-else="">
                  <div style="width:60px;height:60px;border-radius:50%;background:#eee;border:2px solid #e94560;display:flex;align-items:center;justify-content:center;font-size:1.5rem;">👤</div>
                </t>
              </div>
              <!-- Info -->
              <div style="flex:1;">
                <div style="font-size:1rem; font-weight:bold; color:#0f3460; margin-bottom:4px;">
                  <t t-esc="teacher.name"/>
                </div>
                <div style="font-size:0.8rem; color:#555; margin-bottom:2px;">
                  رقم الموظف: <t t-esc="teacher.employee_code"/>
                </div>
                <div style="font-size:0.8rem; color:#555; margin-bottom:2px;">
                  التخصص: <t t-esc="teacher.specialization or '—'"/>
                </div>
                <div style="font-size:0.8rem; color:#555;">
                  المدرسة: <t t-esc="teacher.school_id.name or '—'"/>
                </div>
              </div>
              <!-- QR Code -->
              <div style="flex-shrink:0; text-align:center;">
                <t t-if="teacher.qr_code_image">
                  <img t-att-src="'data:image/png;base64,' + (teacher.qr_code_image.decode('utf-8') if isinstance(teacher.qr_code_image, bytes) else teacher.qr_code_image)"
                       style="width:80px; height:80px;"/>
                </t>
                <div style="font-size:0.65rem; color:#aaa; margin-top:2px;">
                  <t t-esc="teacher.qr_code or ''"/>
                </div>
              </div>
            </div>
            <!-- Footer -->
            <div style="background:#f5f5f5; padding:4px 12px; font-size:0.7rem; color:#aaa; text-align:center; border-top:1px solid #ddd;">
              نظام إدارة المدرسة — رمز QR للحضور
            </div>
          </div>
        </t>
      </t>
    </t>
  </template>

</odoo>
